# SkillBridge AI — Frontend environment variables
# Copy: cp .env.example .env.local  →  fill in values

# Backend API URL — set to your Render backend URL after deploying
VITE_API_URL=https://skillbridge-backend.onrender.com

# For local development (uses Vite proxy defined in vite.config.js):
# VITE_API_URL=http://localhost:10000
